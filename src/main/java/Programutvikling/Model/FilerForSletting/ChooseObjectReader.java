package Programutvikling.Model.FilerForSletting;

/*public class ChooseObjectReader {
    public /*ArrayList<Object>void chooseObjectReaderCSV(File path, String type){
        ArrayList<Object> objectList = null;

       /* try {
            objectList = ObjectReaderCSV.readObjects(path.toString(), type);
        } catch (IOException e) {
            System.err.println("Could not read the requested file. Cause: " + e.getCause());
        } catch (InvalidPersonFormatException e) {
            System.err.println("The data is not formatted correctly. Message: " + e.getMessage());
        }

        if(objectList == null) { // some error has occurred
            System.exit(1);
        }

        System.out.println(objectList);
    }
    public /*ArrayList<Object> void chooseObjectReaderJOBJ(File filepath){
        ArrayList<Object> objectList = new ArrayList<>();
        //ObjectReaderJOBJ objectReader = new ObjectReaderJOBJ();
        try {
            objectList = ObjectReaderJOBJ.readObjects(filepath);
        } catch (IOException e) {
            System.err.println("Could not read the requested file. Cause: " + e.getCause());
        } catch (ClassNotFoundException e) {
            System.err.println("The data is not formatted correctly. Message: " + e.getMessage());
        }

        if(objectList == null) { // some error has occurred
            System.exit(1);
        }

        for(Object e : objectList) {
            System.out.println(e);
        }
    }
}*/
